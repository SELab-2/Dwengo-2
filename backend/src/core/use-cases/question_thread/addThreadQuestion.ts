import { UseCase } from "../../../config/useCase";

export class AddThreadQuestion implements UseCase<undefined, undefined> {
  constructor() {}

  async execute(input: undefined): Promise<undefined> {
    //TODO
  }
}