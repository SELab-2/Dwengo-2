import { UseCase } from "../../../config/useCase";

export class DeleteQuestionThread implements UseCase<undefined, undefined> {
  constructor() {}

  async execute(input: undefined): Promise<undefined> {
    //TODO
  }
}