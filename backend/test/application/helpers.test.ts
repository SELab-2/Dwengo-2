import { Request as ExpressRequest, Response as ExpressResponse } from 'express';
import { requestFromExpress, requestToExpress, responseFromExpress, responseToExpress } from "../../src/application/helpers";
import { Request, Response, HttpMethod } from "../../src/application/types";

describe("Request Objects", (): void => {
  it.todo("can be converted from Express Request objects");  
  it.todo("can be converted to Express Request objects");
});

describe("Response Objects", (): void => {
  it.todo("can be converted from Express Response objects");
  it.todo("can be converted to Express Response objects");
});
