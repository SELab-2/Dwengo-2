export interface LearningPath {
    title: string;
    description: string;
    steps: number;
    minAge: number;
    maxAge: number;
    learningPathId: string;
}

export interface LearningPathRequest {

}